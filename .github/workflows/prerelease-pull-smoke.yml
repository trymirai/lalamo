name: "Backwards Compatibility Tests"

on:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize, labeled, unlabeled, closed]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: prerelease-pull-smoke-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

env:
  GH_TOKEN: ${{ github.token }}
  PR_NUMBER: ${{ github.event.pull_request.number }}

jobs:
  config:
    uses: ./.github/workflows/config.yml

  pull-smoke:
    name: Pull Smoke (CPU)
    runs-on: ubuntu-latest
    needs: [config]
    if: ${{ needs.config.outputs.run-tests == 'true' && needs.config.outputs.is-prerelease == 'true' }}

    steps:
      - uses: actions/checkout@v6

      - uses: ./.github/actions/setup
        with:
          dev: 'true'

      - name: Ensure GitHub CLI is available
        shell: bash
        run: |
          if command -v gh >/dev/null 2>&1; then
            gh --version
            exit 0
          fi

          arch="$(uname -m)"
          case "$arch" in
            x86_64) gh_arch="amd64" ;;
            aarch64|arm64) gh_arch="arm64" ;;
            *)
              echo "Unsupported architecture for gh install: $arch"
              exit 1
              ;;
          esac

          gh_version="2.77.0"
          gh_tgz="gh_${gh_version}_linux_${gh_arch}.tar.gz"
          curl -fsSL "https://github.com/cli/cli/releases/download/v${gh_version}/${gh_tgz}" -o "${gh_tgz}"
          tar -xzf "${gh_tgz}"
          mkdir -p "$HOME/.local/bin"
          cp "gh_${gh_version}_linux_${gh_arch}/bin/gh" "$HOME/.local/bin/gh"
          chmod +x "$HOME/.local/bin/gh"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          gh --version

      - name: Run pull tests
        id: pull_smoke
        continue-on-error: true
        run: |
          pytest tests/indicators/pull_smoke.py 2>&1 | tee pull_smoke.log

      - name: Comment on PR if pull smoke passed
        if: ${{ always() && steps.pull_smoke.outcome == 'success' }}

        shell: bash
        run: |
          gh pr comment "$PR_NUMBER" --body "[CI NOTICE] Pull tests passed. Bumping the minor version is appropriate."

      - name: Comment on PR if pull smoke failed
        if: ${{ always() && steps.pull_smoke.outcome == 'failure' }}
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        shell: bash
        run: |
          if grep -q "pulled model tensor signatures differ from a fresh conversion" pull_smoke.log; then
            notice="[CI NOTICE] Pulled model tensors differ from fresh conversion."
            detail="The pull succeeded, but stored artifact tensor names, shapes, or dtypes do not match a fresh conversion from this checkout."
          elif grep -q "test_pulled_model_generates_adequate_output" pull_smoke.log; then
            notice="[CI NOTICE] A pulled model no longer runs cleanly with current lalamo."
            detail="The direct pull/chat smoke failed, so pulled artifacts are not runtime-compatible with this release."
          else
            notice="[CI NOTICE] Pull tests failed."
            detail="The pull smoke failed before the failure could be classified. Check the pytest output below."
          fi

          {
            echo "$notice"
            echo
            echo "$detail"
            echo "Please consider bumping the major version for this release, and updating all the server-stored models."
            echo
            echo "<details><summary>Error output (last 200 lines)</summary>"
            echo
            echo '```text'
            tail -n 200 pull_smoke.log
            echo '```'
            echo
            echo "</details>"
          } > pr_pull_smoke_comment.md

          gh pr comment "$PR_NUMBER" --body-file pr_pull_smoke_comment.md
