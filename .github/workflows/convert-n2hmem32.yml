name: "Convert (n2hmem32)"

on:
  workflow_dispatch:
  pull_request:
    types: [opened, reopened, ready_for_review, synchronize, closed, labeled]

permissions:
  contents: read

defaults:
  run:
    shell: bash

env:
  HF_TOKEN: ${{ secrets.HF_TOKEN }}

jobs:
  convert:
    name: Convert (Qwen3-Next-80B-A3B)
    runs-on: [github-n2hmem32]

    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup

      - name: Convert model
        run: |
          lalamo convert "Qwen/Qwen3-Next-80B-A3B-Instruct"
